[package]
name = "llama-harness-desktop"
version = "0.1.0"
description = "Desktop shell for the local Llama Harness operator console."
authors = ["tyhuang"]
edition = "2021"

[lib]
name = "llama_harness_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
